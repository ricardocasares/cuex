export const exchange = ({ exchange }) => exchange;
export const rate = ({ exchange: { rate } }) => rate;
export const targetSymbol = ({ exchange: { targetSymbol } }) => targetSymbol;
export const originSymbol = ({ exchange: { originSymbol } }) => originSymbol;
